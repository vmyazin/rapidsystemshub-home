type SiteConfig = {
  image: string,
  description: string
}

export const siteConfig: SiteConfig = {
  image: "social.jpg",
  description: "Digital marketing enterprise offering a tailored approach to lead generation and ad campaign management."
};